#include "main.h"

/**
* _strcmp - compares strings
* @s1: first pointer
* @s2: second pointer
* Return: 0
*/

int _strcmp(char *s1, char *s2)
{

if (_strcmp(s1, s2) == 0)

return (0);

else

return (s1 - s2);
}
